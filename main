import { useState, useEffect } from "react";
const DEFAULT_PLAYER = {
age: 13, height: "5'5\"", weight: 138,
position: "Running Back", seasonStart: "Sep 9, 2026",
};
const SEASON_DATE = new Date("2026-09-09");
const PHASES = [
{ name: "FOUNDATION", range: "May – Jun", color: "#22c55e", desc: "Build your base. Fix wea
{ name: "POWER UP", range: "July", color: "#f5c842", desc: "Ramp intensity. Explosive speed
{ name: "GAME SHAPE", range: "August", color: "#ef4444", desc: "Sharp cuts, conditioning, a
];
const PROGRAM = {
Monday: {
label: "SPEED & EXPLOSION", icon: " ", color: "#f5c842",
focus: "First 10 yards wins every run.",
exercises: [
{ name: "Dynamic Warm-Up", sets: 1, reps: "5 min", tip: "High knees, butt kicks, leg sw
{ name: "10-Yard Burst Starts", sets: 10, reps: "Full effort", tip: "Low stance, explod
{ name: "40-Yard Dash", sets: 4, reps: "Timed", tip: "Drive knees up, pump arms straigh
{ name: "Broad Jumps", sets: 4, reps: "5 reps", tip: "Explode forward, land soft. Pure
{ name: "High Knees", sets: 3, reps: "30 sec", tip: "Fast feet, drive knees to chest. S
{ name: "Hip Flexor Stretch", sets: 3, reps: "45s each", tip: "Tight hips = slow player
]
},
Tuesday: {
label: "LOWER BODY POWER", icon: " ", color: "#f97316",
focus: "RBs break tackles with leg strength.",
exercises: [
{ name: "Bodyweight Squats", sets: 4, reps: "20 reps", tip: "Chest up, knees over toes.
{ name: "Bulgarian Split Squats", sets: 3, reps: "12 each leg", tip: "Back foot on chai
{ name: "Glute Bridges", sets: 4, reps: "20 reps", tip: "Squeeze hard at top for 1s. Gl
{ name: "Walking Lunges", sets: 3, reps: "20 total", tip: "Long stride, stay balanced."
{ name: "Calf Raises", sets: 3, reps: "30 reps", tip: "Slow and controlled. Ankle stren
{ name: "Wall Sit", sets: 3, reps: "60 sec", tip: "When it burns that's when it counts.
]
},
Wednesday: {
label: "AGILITY & CUTS", icon: " ", color: "#8b5cf6",
focus: "Make defenders miss. RBs don't run straight.",
exercises: [
{ name: "Cone Shuttle (5-10-5)", sets: 6, reps: "Timed", tip: "Plant hard on direction
{ name: "Ladder Drills", sets: 4, reps: "3 min", tip: "Fast feet. In-in-out-out pattern
{ name: "Defensive Cone Weave", sets: 5, reps: "Full speed", tip: "Set up 5 cones, weav
{ name: "Lateral Shuffle", sets: 4, reps: "30 sec", tip: "Stay low, fast feet, don't cr
{ name: "Jump Rope", sets: 4, reps: "1 min", tip: "Best coordination builder. Cheap and
{ name: "Hamstring Stretch", sets: 3, reps: "45s each", tip: "Tight hamstrings = pulled
]
},
Thursday: {
label: "UPPER BODY STRENGTH", icon: " ", color: "#3b82f6",
focus: "Strong arms stiff-arm defenders. Strong core = better balance.",
exercises: [
{ name: "Push-Ups", sets: 4, reps: "20 reps", tip: "2 seconds down, explode up. Perfect
{ name: "Diamond Push-Ups", sets: 3, reps: "12 reps", tip: "Elbows tight. Builds tricep
{ name: "Pike Push-Ups", sets: 3, reps: "12 reps", tip: "Hips high, head toward floor.
{ name: "Plank", sets: 3, reps: "60 sec", tip: "Tight core, straight line head to heels
{ name: "Superman Hold", sets: 3, reps: "15 reps", tip: "Back strength prevents injurie
{ name: "Mountain Climbers", sets: 3, reps: "30 sec", tip: "Core + cardio. Drive knees
]
},
Friday: {
label: "FULL SPEED CONDITIONING", icon: " ", color: "#ef4444",
focus: "4th quarter legs. When everyone else is tired, you're just getting started.",
exercises: [
{ name: "100-Yard Sprints", sets: 5, reps: "Full effort", tip: "Game speed every rep. R
{ name: "Suicide Runs", sets: 4, reps: "Full field", tip: "10, 20, 30, 40 yards and bac
{ name: "Bear Crawls", sets: 3, reps: "20 yards", tip: "Hips low, move fast. Full body
{ name: "Burpees", sets: 3, reps: "15 reps", tip: "Explosive jump at top. Don't cheat t
{ name: "Box Jumps (use a step)", sets: 4, reps: "8 reps", tip: "Land soft, reset, expl
{ name: "Cool Down Walk", sets: 1, reps: "5 min", tip: "Breathe, walk, let your heart r
]
},
Saturday: {
label: "BALL SKILLS & IQ", icon: " ", color: "#22c55e",
focus: "Elite RBs don't just run fast. They think fast.",
exercises: [
{ name: "Ball Security Drills", sets: 4, reps: "5 min", tip: "High and tight. Four poin
{ name: "Stiff Arm Practice", sets: 3, reps: "10 each arm", tip: "Extend arm full, driv
{ name: "Juke & Cut Practice", sets: 5, reps: "10 reps", tip: "Plant foot hard, sell th
{ name: "Catch Routes (if partner)", sets: 3, reps: "10 catches", tip: "RBs who catch o
{ name: "Watch NFL Film", sets: 1, reps: "20 min", tip: "Watch Derrick Henry, Christian
{ name: "Full Body Stretch", sets: 1, reps: "10 min", tip: "Head to toe. Flexibility =
]
},
Sunday: {
label: "REST & RECOVERY", icon: " ", color: "#64748b",
focus: "Recovery is training. You grow on rest days.",
exercises: [
{ name: "Sleep 9+ Hours", sets: 1, reps: "All night", tip: "Growth hormone releases whi
{ name: "Light Walk", sets: 1, reps: "20 min", tip: "Keep blood flowing without stressi
{ name: "Full Body Stretch", sets: 1, reps: "15 min", tip: "Hold each stretch 30-45 sec
{ name: "Eat Well", sets: 1, reps: "All day", tip: "Eggs, chicken, rice, milk, peanut b
{ name: "Mental Visualization", sets: 1, reps: "10 min", tip: "Close your eyes. See you
{ name: "Hydrate", sets: 1, reps: "All day", tip: "Half your bodyweight in ounces of wa
]
},
};
const DAYS = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"];
const NFL_BENCHMARKS = [
{ stat: "40-Yard Dash", nfl: "4.45s", yours: "Work toward 5.0s", icon: " " },
{ stat: "Vertical Jump", nfl: "36 inches", yours: "Work toward 24 inches", icon: "↑" },
{ stat: "Broad Jump", nfl: "10 ft 4 in", yours: "Work toward 7 ft 6 in", icon: "→" },
{ stat: "3-Cone Drill", nfl: "6.7s", yours: "Work toward 8.0s", icon: " " },
{ stat: "Weight", nfl: "215 lbs", yours: "138 lbs → grow naturally", icon: " " },
];
const NUTRITION = [
{
meal: "Breakfast", why: "Protein + carbs to fuel morning training",
items: [
{ name: "Eggs (dozen)", price: "$2.49", store: "Food Lion store brand" },
{ name: "White bread loaf", price: "$1.29", store: "Food Lion store brand" },
{ name: "Gallon whole milk", price: "$3.49", store: "Food Lion store brand" },
{ name: "Bananas (per lb)", price: "$0.59", store: "Food Lion produce" },
],
recipe: "Scramble 3 eggs, toast 2 slices of bread, pour a glass of milk, eat a banana. Do
totalPerMeal: "~$1.20 per meal",
},
{
meal: "Lunch", why: "Lean protein builds muscle, carbs give energy",
items: [
{ name: "Chicken breast (2 lb bag)", price: "$5.99", store: "Food Lion" },
{ name: "White rice (5 lb bag)", price: "$3.49", store: "Food Lion store brand" },
{ name: "Frozen mixed veggies (12oz)", price: "$1.29", store: "Food Lion store brand" }
],
recipe: "Cook rice (makes many servings). Season and bake or pan fry chicken. Microwave v
totalPerMeal: "~$1.50 per meal",
},
{
meal: "Snack", why: "Keep your metabolism fueled between meals",
items: [
{ name: "Peanut butter (16oz jar)", price: "$2.99", store: "Food Lion store brand" },
{ name: "Apples (3 lb bag)", price: "$3.49", store: "Food Lion produce" },
],
recipe: "Slice apple, dip in peanut butter. Quick, easy, keeps you full.",
totalPerMeal: "~$0.70 per snack",
},
{
meal: "Dinner", why: "Biggest recovery meal. Eat within 1hr of training.",
items: [
{ name: "Ground beef 80/20 (1 lb)", price: "$4.99", store: "Food Lion" },
{ name: "Pasta (1 lb box)", price: "$1.19", store: "Food Lion store brand" },
{ name: "Pasta sauce (24oz jar)", price: "$1.79", store: "Food Lion store brand" { name: "Frozen broccoli (12oz)", price: "$1.29", store: "Food Lion store brand" },
},
],
recipe: "Brown ground beef, boil pasta, heat sauce, mix together. Microwave broccoli on t
totalPerMeal: "~$2.00 per meal",
},
{
meal: "Before Bed", why: "Slow protein feeds muscles while you sleep",
items: [
{ name: "Gallon whole milk", price: "$3.49", store: "Food Lion store brand (shared with
{ name: "Greek yogurt (32oz)", price: "$4.99", store: "Food Lion store brand" },
],
recipe: "Glass of cold milk OR a cup of Greek yogurt. Either one works. Don't skip totalPerMeal: "~$0.60 per night",
this."
},
];
const RB_GREATS = [
{ name: "Darren Sproles", height: "5'6\"", note: "Smaller than you at his peak. 3x Pro Bowl
{ name: "Barry Sanders", height: "5'8\"", note: "Possibly the greatest RB ever. Pure agilit
{ name: "Emmitt Smith", height: "5'9\"", note: "All-time NFL rushing leader. Wasn't the fas
{ name: "Maurice Jones-Drew", height: "5'7\"", note: "Low center of gravity = impossible to
];
export default function App() {
const [tab, setTab] = useState("Home");
const [selectedDay, setSelectedDay] = useState("Monday");
const [completed, setCompleted] = useState({});
const [stats, setStats] = useState({ fortyTime: "", vertJump: "", weight: 138 });
const [daysLeft, setDaysLeft] = useState(0);
const [streak, setStreak] = useState(0);
const [profile, setProfile] = useState(DEFAULT_PLAYER);
const [editingProfile, setEditingProfile] = useState(false);
const [profileDraft, setProfileDraft] = useState(DEFAULT_PLAYER);
useEffect(() => {
const today = new Date();
const diff = Math.ceil((SEASON_DATE - today) / (1000 * 60 * 60 * 24));
setDaysLeft(diff);
const todayName = DAYS[today.getDay() === 0 ? 6 : today.getDay() - 1];
setSelectedDay(todayName);
const saved = localStorage.getItem("rb_completed");
if (saved) {
const parsed = JSON.parse(saved);
const today = new Date().toISOString().split("T")[0];
if (parsed.date === today) {
setCompleted(parsed.data);
} else {
localStorage.removeItem("rb_completed");
}
}
const savedStats = localStorage.getItem("rb_stats");
if (savedStats) setStats(JSON.parse(savedStats));
const savedStreak = localStorage.getItem("rb_streak");
if (savedStreak) setStreak(parseInt(savedStreak));
const savedProfile = localStorage.getItem("rb_profile");
if (savedProfile) {
const p = JSON.parse(savedProfile);
setProfile(p);
setProfileDraft(p);
}
}, []);
const getTodayDate = () => new Date().toISOString().split("T")[0];
const toggleExercise = (day, idx) => {
const key = `${day}_${idx}`;
const next = { ...completed, [key]: !completed[key] };
setCompleted(next);
localStorage.setItem("rb_completed", JSON.stringify({ date: getTodayDate(), data: next })
};
const saveStats = () => {
localStorage.setItem("rb_stats", JSON.stringify(stats));
};
const saveProfile = () => {
setProfile(profileDraft);
localStorage.setItem("rb_profile", JSON.stringify(profileDraft));
setEditingProfile(false);
};
const getTodayDay = () => {
return DAYS[new Date().getDay() === 0 ? 6 : new Date().getDay() - 1];
};
const getPhase = () => {
const month = new Date().getMonth();
if (month <= 5) return PHASES[0];
if (month === 6) return PHASES[1];
return PHASES[2];
};
const phase = getPhase();
const todayDay = getTodayDay();
const todayWorkout = PROGRAM[todayDay];
const selectedWorkout = PROGRAM[selectedDay];
const completedToday = selectedWorkout.exercises.filter((_, i) =>
completed[`${selectedDay}_${i}`]
).length;
const totalToday = selectedWorkout.exercises.length;
const pct = Math.round((completedToday / totalToday) * 100);
const tabs = ["Home", "Today", "Program", "Benchmarks", "Nutrition", "Legends"];
return (
<div style={{
minHeight: "100vh",
background: "#08090d",
color: "#f0f0f0",
fontFamily: "'Barlow Condensed', 'Oswald', sans-serif",
paddingBottom: 80,
}}>
<link href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@400;600;700;
{/* Header */}
<div style={{
background: "linear-gradient(135deg, #0f1117 0%, #1a1a2e 100%)",
borderBottom: "2px solid #f5c842",
padding: "16px 20px 12px",
position: "sticky", top: 0, zIndex: 100,
}}>
<div style={{ display: "flex", justifyContent: "space-between", alignItems: "center"
<div>
</div>
<div style={{ textAlign: "right" }}>
{daysLeft > 0 ? (
<>
<div style={{ fontSize: 11, color: "#f5c842", letterSpacing: 3, fontWeight: 700 }
<div style={{ fontSize: 28, fontWeight: 900, letterSpacing: 2, lineHeight: 1 }}>N
<div style={{
background: "#f5c842", color: "#000", borderRadius: 8,
padding: "6px 12px", fontWeight: 900, fontSize: 14,
}}>
{daysLeft} DAYS
</div>
</>
) : daysLeft === 0 ? (
<>
<div style={{ fontSize: 10, color: "#666", marginTop: 2 }}>until season</div>
<div style={{
background: "#ef4444", color: "#fff", borderRadius: 8,
padding: "6px 12px", fontWeight: 900, fontSize: 14,
animation: "pulse 1s infinite",
}}>
GAME DAY
</div>
<div style={{ fontSize: 10, color: "#ef4444", marginTop: 2 }}>IT'S TIME</div>
</>
) : (
<>
<div style={{
background: "#22c55e", color: "#000", borderRadius: 8,
padding: "6px 12px", fontWeight: 900, fontSize: 14,
}}>
IN SEASON
</div>
</>
<div style={{ fontSize: 10, color: "#22c55e", marginTop: 2 }}>KEEP GRINDING</
)}
</div>
</div>
</div>
{/* Tab Content */}
<div style={{ padding: "16px 16px 0" }}>
{tab === "Home" && (
<div>
{/* Game Day Banner */}
{daysLeft === 0 && (
<div style={{
background: "linear-gradient(135deg, #7f0000, #ef4444)",
borderRadius: 16, padding: 24, marginBottom: 16,
textAlign: "center", border: "2px solid #ef4444",
}}>
<div style={{ fontSize: 48 }}> </div>
<div style={{ fontSize: 36, fontWeight: 900, letterSpacing: 2 }}>GAME DAY!</d
<div style={{ fontSize: 14, color: "#fca5a5", marginTop: 8, fontFamily: "Barl
Everything you did this summer was for TODAY. Go take what's yours.
</div>
</div>
)}
{/* In Season Banner */}
{daysLeft < 0 && (
<div style={{
background: "linear-gradient(135deg, #052e16, #22c55e22)",
borderRadius: 16, padding: 20, marginBottom: 16,
textAlign: "center", border: "2px solid #22c55e",
}}>
</div>
</div>
<div style={{ fontSize: 32, fontWeight: 900, color: "#22c55e" }}> IN SEASON
<div style={{ fontSize: 13, color: "#86efac", marginTop: 6, fontFamily: "Barl
Season started {Math.abs(daysLeft)} days ago. Stay healthy, keep grinding,
)}
{/* Player Card */}
<div style={{
background: "linear-gradient(135deg, #1a1a2e, #16213e)",
border: "1px solid #f5c842",
borderRadius: 16, padding: 20, marginBottom: 16,
}}>
<div style={{ display: "flex", justifyContent: "space-between", alignItems: "ce
<div style={{ fontSize: 11, color: "#f5c842", letterSpacing: 3 }}>YOUR PROFIL
<button onClick={() => { setProfileDraft(profile); setEditingProfile(!editing
background: editingProfile ? "#ef4444" : "#1e2a4a", border: "1px solid #f5c
color: "#f5c842", borderRadius: 6, padding: "4px 10px",
fontSize: 11, fontWeight: 700, cursor: "pointer", fontFamily: "inherit", le
}}>
</button>
</div>
{editingProfile ? "CANCEL" : " EDIT"}
{!editingProfile ? (
<>
<div style={{ fontSize: 32, fontWeight: 900 }}>RUNNING BACK</div>
<div style={{ display: "flex", gap: 20, marginTop: 12, flexWrap: "wrap" }}>
{[
{ label: "AGE", val: profile.age },
{ label: "HEIGHT", val: profile.height },
{ label: "WEIGHT", val: `${profile.weight} lbs` },
{ label: "GOAL", val: "NFL" },
].map(s => (
<div key={s.label}>
<div style={{ fontSize: 10, color: "#888", letterSpacing: 2 }}>{s.lab
<div style={{ fontSize: 18, fontWeight: 700, color: "#f5c842" }}>{s.v
</div>
))}
</div>
</>
) : (
<div style={{ marginTop: 12 }}>
{[
{ label: "AGE", key: "age", placeholder: "13" },
{ label: "HEIGHT (e.g. 5'6\")", key: "height", placeholder: "5'5\"" },
{ label: "WEIGHT (lbs)", key: "weight", placeholder: "138" },
].map(f => (
<div key={f.key} style={{ marginBottom: 10 }}>
<div style={{ fontSize: 10, color: "#888", letterSpacing: 2, marginBott
<input
value={profileDraft[f.key]}
onChange={e => setProfileDraft({ ...profileDraft, [f.key]: e.target.v
placeholder={f.placeholder}
style={{
background: "#0f1525", border: "1px solid #f5c842",
borderRadius: 8, padding: "10px 14px", color: "#f0f0f0",
fontSize: 16, fontFamily: "inherit", width: "100%", boxSizing: "bor
}}
/>
</div>
))}
<button onClick={saveProfile} style={{
background: "#f5c842", color: "#000", border: "none",
borderRadius: 8, padding: "12px", fontWeight: 900,
fontSize: 14, cursor: "pointer", width: "100%",
fontFamily: "inherit", letterSpacing: 1, marginTop: 4,
}}>
SAVE PROFILE ✓
</button>
</div>
)}
</div>
{/* Current Phase */}
<div style={{
background: "#111118", border: `1px solid ${phase.color}`,
borderRadius: 12, padding: 16, marginBottom: 16,
}}>
<div style={{ fontSize: 10, color: "#888", letterSpacing: 3 }}>CURRENT PHASE</d
<div style={{ fontSize: 24, fontWeight: 900, color: phase.color }}>{phase.name}
<div style={{ fontSize: 13, color: "#bbb", marginTop: 4 }}>{phase.range} — {pha
</div>
{/* Today's Preview */}
<div style={{
background: "#111118", border: "1px solid #1e1e2e",
borderRadius: 12, padding: 16, marginBottom: 16,
}}>
<div style={{ fontSize: 10, color: "#888", letterSpacing: 3, marginBottom: 8 }}
<div style={{ display: "flex", alignItems: "center", gap: 10, marginBottom: 8 }
<span style={{ fontSize: 28 }}>{todayWorkout.icon}</span>
<div>
<div style={{ fontSize: 20, fontWeight: 900, color: todayWorkout.color }}>{
<div style={{ fontSize: 12, color: "#999" }}>{todayWorkout.focus}</div>
</div>
</div>
<button onClick={() => { setSelectedDay(todayDay); setTab("Today"); }} style={{
background: "#f5c842", color: "#000", border: "none",
borderRadius: 8, padding: "10px 20px", fontWeight: 900,
fontSize: 14, cursor: "pointer", width: "100%",
fontFamily: "inherit", letterSpacing: 1,
}}>
START TODAY'S WORKOUT →
</button>
</div>
{/* Motivation */}
{(() => {
const QUOTES = [
"Darren Sproles was 5'6\". Maurice Jones-Drew was 5'7\". Size didn't stop the
"The NFL doesn't care where you started. It cares where you finished. Finish
"Your future teammates are training right now. Are you?",
"4th quarter is where careers are made. Build your conditioning now so you're
"Film doesn't lie. Every game you play in high school is a scout watching. Ma
"Sleep, eat, train. That's the job. Do it every day and doors will open.",
"Barry Sanders wasn't the biggest. He was the most prepared. Be the most prep
];
const [quoteIdx, setQuoteIdx] = useState(0);
return (
<div onClick={() => setQuoteIdx((quoteIdx + 1) % QUOTES.length)} style={{
background: "linear-gradient(135deg, #1a0a00, #2d1500)",
border: "1px solid #f97316", borderRadius: 12, padding: 16, marginBottom: 1
cursor: "pointer",
}}>
DAILY
<div style={{ display: "flex", justifyContent: "space-between", alignItems:
<div style={{ fontSize: 13, color: "#f97316", fontWeight: 700 }}> <div style={{ fontSize: 10, color: "#f97316", opacity: 0.6 }}>TAP FOR NEX
</div>
<div style={{ fontSize: 15, fontWeight: 600, lineHeight: 1.5, fontFamily: "
"{QUOTES[quoteIdx]}"
</div>
<div style={{ display: "flex", gap: 4, marginTop: 10 }}>
{QUOTES.map((_, i) => (
<div key={i} style={{
flex: 1, height: 3, borderRadius: 2,
background: i === quoteIdx ? "#f97316" : "#3a2000",
transition: "background 0.3s",
}} />
))}
</div>
</div>
);
})()}
{/* Phase Timeline */}
<div style={{ background: "#111118", borderRadius: 12, padding: 16 }}>
<div style={{ fontSize: 10, color: "#888", letterSpacing: 3, marginBottom: 12 }
{PHASES.map((p, i) => (
<div key={i} style={{ display: "flex", gap: 12, marginBottom: 12, alignItems:
<div style={{
width: 12, height: 12, borderRadius: "50%",
background: p.color, marginTop: 4, flexShrink: 0,
}} />
<div>
<div style={{ fontWeight: 700, color: p.color, fontSize: 15 }}>{p.name} <
<div style={{ fontSize: 12, color: "#999" }}>{p.desc}</div>
</div>
</div>
))}
</div>
</div>
)}
{tab === "Today" && (
<div>
{/* Day Selector */}
<div style={{ display: "flex", gap: 6, overflowX: "auto", paddingBottom: 8, margi
{DAYS.map(d => (
<button key={d} onClick={() => setSelectedDay(d)} style={{
background: selectedDay === d ? PROGRAM[d].color : "#111118",
color: selectedDay === d ? "#000" : "#888",
border: `1px solid ${selectedDay === d ? PROGRAM[d].color : "#1e1e2e"}`,
borderRadius: 8, padding: "8px 12px", fontWeight: 700,
fontSize: 11, cursor: "pointer", whiteSpace: "nowrap",
fontFamily: "inherit", letterSpacing: 1,
}}>
{d.slice(0, 3).toUpperCase()}
{d === todayDay && <span style={{ marginLeft: 4 }}>●</span>}
</button>
))}
</div>
{/* Workout Header */}
<div style={{
background: "#111118", border: `2px solid ${selectedWorkout.color}`,
borderRadius: 16, padding: 16, marginBottom: 16,
}}>
<div style={{ display: "flex", justifyContent: "space-between", alignItems: "fl
<div>
<div style={{ fontSize: 11, color: "#888", letterSpacing: 3 }}>{selectedDay
<div style={{ fontSize: 24, fontWeight: 900, color: selectedWorkout.color }
{selectedWorkout.icon} {selectedWorkout.label}
</div>
<div style={{ fontSize: 12, color: "#bbb", marginTop: 4 }}>{selectedWorkout
</div>
<div style={{ textAlign: "right" }}>
<div style={{ fontSize: 28, fontWeight: 900, color: selectedWorkout.color }
<div style={{ fontSize: 10, color: "#666" }}>{completedToday}/{totalToday}<
</div>
</div>
{/* Progress bar */}
<div style={{ background: "#1e1e2e", borderRadius: 4, height: 6, marginTop: 12
<div style={{
background: selectedWorkout.color, height: 6, borderRadius: 4,
width: `${pct}%`, transition: "width 0.3s",
}} />
</div>
</div>
{/* Exercise List */}
{selectedWorkout.exercises.map((ex, i) => {
const key = `${selectedDay}_${i}`;
const done = completed[key];
return (
<div key={i} onClick={() => toggleExercise(selectedDay, i)} style={{
background: done ? "#0d1f0d" : "#111118",
border: `1px solid ${done ? "#22c55e" : "#1e1e2e"}`,
borderRadius: 12, padding: 14, marginBottom: 10,
cursor: "pointer", transition: "all 0.2s",
}}>
<div style={{ display: "flex", justifyContent: "space-between", alignItems:
<div style={{ flex: 1 }}>
<div style={{ display: "flex", alignItems: "center", gap: 8, marginBott
<div style={{
width: 22, height: 22, borderRadius: "50%",
border: `2px solid ${done ? "#22c55e" : "#444"}`,
background: done ? "#22c55e" : "transparent",
display: "flex", alignItems: "center", justifyContent: "center",
fontSize: 12, flexShrink: 0,
}}>
{done ? "✓" : ""}
</div>
<div style={{
fontWeight: 700, fontSize: 16,
color: done ? "#22c55e" : "#f0f0f0",
textDecoration: done ? "line-through" : "none",
}}>{ex.name}</div>
</div>
<div style={{ display: "flex", gap: 12, marginLeft: 30, marginBottom: 6
<span style={{
background: "#1e1e2e", borderRadius: 4,
padding: "2px 8px", fontSize: 11, color: "#aaa",
}}>
{ex.sets} sets
</span>
<span style={{
background: "#1e1e2e", borderRadius: 4,
padding: "2px 8px", fontSize: 11, color: "#aaa",
}}>
{ex.reps}
</span>
</div>
<div style={{
fontSize: 12, color: "#f5c842", marginLeft: 30,
fontFamily: "Barlow, sans-serif",
}}>
{ex.tip}
</div>
</div>
</div>
</div>
);
})}
{pct === 100 && (
<div style={{
background: "linear-gradient(135deg, #0d1f0d, #1a3d1a)",
border: "2px solid #22c55e", borderRadius: 16,
padding: 20, textAlign: "center", marginTop: 8,
}}>
<div style={{ fontSize: 40 }}> </div>
<div style={{ fontSize: 24, fontWeight: 900, color: "#22c55e" }}>WORKOUT COMP
<div style={{ fontSize: 13, color: "#bbb", marginTop: 4, fontFamily: "Barlow,
Another day closer to the NFL. Rest, eat, sleep.
</div>
</div>
)}
</div>
)}
16 }}>
{tab === "Program" && (
<div>
<div style={{ fontSize: 11, color: "#888", letterSpacing: 3, marginBottom: {DAYS.map(day => {
const w = PROGRAM[day];
const done = w.exercises.filter((_, i) => completed[`${day}_${i}`]).length;
return (
<div key={day} onClick={() => { setSelectedDay(day); setTab("Today"); }} styl
background: "#111118", border: `1px solid #1e1e2e`,
borderRadius: 12, padding: 14, marginBottom: 10, cursor: "pointer",
borderLeft: `4px solid ${w.color}`,
}}>
<div style={{ display: "flex", justifyContent: "space-between", alignItems:
<div>
<div style={{ fontSize: 11, color: "#666", letterSpacing: 2 }}>{day.toU
<div style={{ fontSize: 18, fontWeight: 700, color: w.color }}>{w.icon}
<div style={{ fontSize: 11, color: "#999", marginTop: 2 }}>{w.focus}</d
</div>
<div style={{ textAlign: "right" }}>
<div style={{ fontSize: 13, color: done === w.exercises.length ? {done}/{w.exercises.length}
</div>
<div style={{ fontSize: 10, color: "#555" }}>done</div>
</div>
</div>
</div>
"#22c5
);
})}
</div>
)}
{tab === "Benchmarks" && (() => {
// Percentile calculators based on real 8th grade RB averages
const getFortyPercentile = (t) => {
const val = parseFloat(t);
if (!val) return null;
if (val <= 4.8) return { pct: 99, label: "ELITE", color: "#f5c842" };
if (val <= 5.0) return { pct: 92, label: "ELITE", color: "#f5c842" };
if (val <= 5.2) return { pct: 80, label: "GREAT", color: "#22c55e" };
if (val <= 5.4) return { pct: 65, label: "ABOVE AVG", color: "#22c55e" };
if (val <= 5.6) return { pct: 50, label: "AVERAGE", color: "#f97316" };
if (val <= 5.8) return { pct: 35, label: "BELOW AVG", color: "#f97316" };
if (val <= 6.0) return { pct: 20, label: "NEEDS WORK", color: "#ef4444" };
return { pct: 10, label: "NEEDS WORK", color: "#ef4444" };
};
const getVertPercentile = (v) => {
const val = parseFloat(v);
if (!val) return null;
if (val >= 30) return { pct: 99, label: "ELITE", color: "#f5c842" };
if (val >= 26) return { pct: 90, label: "ELITE", color: "#f5c842" };
if (val >= 22) return { pct: 75, label: "GREAT", color: "#22c55e" };
if (val >= 19) return { pct: 60, label: "ABOVE AVG", color: "#22c55e" };
if (val >= 16) return { pct: 45, label: "AVERAGE", color: "#f97316" };
if (val >= 13) return { pct: 25, label: "BELOW AVG", color: "#f97316" };
return { pct: 10, label: "NEEDS WORK", color: "#ef4444" };
};
const getBroadPercentile = (v) => {
const val = parseFloat(v);
if (!val) return null;
if (val >= 96) return { pct: 99, label: "ELITE", color: "#f5c842" };
if (val >= 84) return { pct: 88, label: "ELITE", color: "#f5c842" };
if (val >= 78) return { pct: 75, label: "GREAT", color: "#22c55e" };
if (val >= 72) return { pct: 60, label: "ABOVE AVG", color: "#22c55e" };
if (val >= 66) return { pct: 45, label: "AVERAGE", color: "#f97316" };
if (val >= 60) return { pct: 25, label: "BELOW AVG", color: "#f97316" };
return { pct: 10, label: "NEEDS WORK", color: "#ef4444" };
};
const getBenchPercentile = (v) => {
const val = parseFloat(v);
if (!val) return null;
if (val >= 20) return { pct: 99, label: "ELITE", color: "#f5c842" };
if (val >= 15) return { pct: 88, label: "GREAT", color: "#22c55e" };
if (val >= 10) return { pct: 70, label: "ABOVE AVG", color: "#22c55e" };
if (val >= 7) return { pct: 50, label: "AVERAGE", color: "#f97316" };
if (val >= 4) return { pct: 30, label: "BELOW AVG", color: "#f97316" };
return { pct: 10, label: "NEEDS WORK", color: "#ef4444" };
};
const getFortyTips = (t) => {
const val = parseFloat(t);
if (!val) return [];
if (val <= 5.0) return ["Keep doing burst starts 3x week", "Work on your arm driv
if (val <= 5.4) return ["10-yard burst starts every Monday", "Hip flexor stretche
if (val <= 5.8) return ["Sprint 3x per week minimum — full effort only", "Fix you
return ["Sprint full speed 4-5x per week", "Work on your drive phase — stay low f
};
const getVertTips = (v) => {
const val = parseFloat(v);
if (!val) return [];
if (val >= 26) return ["Maintain with weekly box jumps", "Add depth jumps — step
if (val >= 19) return ["Box jumps 3x week", "Broad jumps build the same explosive
return ["Start with glute bridges and squats to build your base", "Practice jumpi
};
const STAT_FIELDS = [
{ label: "40-Yard Dash", key: "fortyTime", unit: "sec", placeholder: "e.g. 5.4",
{ label: "Vertical Jump", key: "vertJump", unit: "inches", placeholder: "e.g. 20"
{ label: "Broad Jump", key: "broadJump", unit: "inches", placeholder: "e.g. 72",
{ label: "Push-Up Reps (max)", key: "benchReps", unit: "reps", placeholder: "e.g.
];
return (
<div>
</div>
<div style={{ fontSize: 11, color: "#888", letterSpacing: 3, marginBottom: 4 }}
<div style={{ fontSize: 12, color: "#666", marginBottom: 16, fontFamily: Enter your numbers. See exactly where you rank vs 8th grade RBs and what to d
"Barlo
{STAT_FIELDS.map((f, i) => {
const result = f.fn(stats[f.key]);
const tips = f.tipFn ? f.tipFn(stats[f.key]) : [];
return (
<div key={i} style={{
background: "#111118", border: `1px solid ${result ? result.color + "44"
borderRadius: 16, padding: 16, marginBottom: 14,
}}>
{/* Stat name + input */}
<div style={{ display: "flex", gap: 10, alignItems: "flex-end", marginBot
<div style={{ flex: 1 }}>
<div style={{ fontSize: 11, color: "#888", letterSpacing: 2, marginBo
<input
value={stats[f.key] || ""}
onChange={e => {
const next = { ...stats, [f.key]: e.target.value };
setStats(next);
localStorage.setItem("rb_stats", JSON.stringify(next));
}}
placeholder={f.placeholder}
style={{
background: "#1a1a2e", border: `1px solid ${result ? result.color
borderRadius: 8, padding: "10px 14px", color: "#f0f0f0",
fontSize: 18, fontFamily: "inherit", fontWeight: 700,
width: "100%", boxSizing: "border-box",
}}
/>
</div>
</div>
<div style={{ fontSize: 12, color: "#666", paddingBottom: 12 }}>{f.unit
{/* Percentile result */}
{result && (
<>
<div style={{ display: "flex", justifyContent: "space-between", align
<div>
<div style={{ fontSize: 11, color: "#888", letterSpacing: 2 }}>VS
<div style={{ fontSize: 26, fontWeight: 900, color: result.color
TOP {100 - result.pct + 1}% — {result.label}
</div>
</div>
<div style={{
width: 56, height: 56, borderRadius: "50%",
border: `3px solid ${result.color}`,
display: "flex", alignItems: "center", justifyContent: "center",
flexDirection: "column",
}}>
<div style={{ fontSize: 16, fontWeight: 900, color: result.color
<div style={{ fontSize: 8, color: "#666" }}>PCT</div>
</div>
</div>
{/* Bar */}
<div style={{ background: "#1e1e2e", borderRadius: 4, height: 8, marg
<div style={{
background: result.color, height: 8, borderRadius: 4,
width: `${result.pct}%`, transition: "width 0.5s",
}} />
</div>
{/* Benchmarks row */}
<div style={{ display: "flex", gap: 8, marginBottom: tips.length ? 12
<div style={{ flex: 1, background: "#1a1400", borderRadius: 8, padd
<div style={{ fontSize: 9, color: "#f5c842", letterSpacing: 2 }}>
<div style={{ fontSize: 13, fontWeight: 700, color: "#f5c842" }}>
</div>
<div style={{ flex: 1, background: "#0d1f0d", borderRadius: 8, padd
<div style={{ fontSize: 9, color: "#22c55e", letterSpacing: 2 }}>
<div style={{ fontSize: 13, fontWeight: 700, color: "#22c55e" }}>
</div>
</div>
{/* Tips */}
{tips.length > 0 && (
<div style={{ background: "#0a0a14", borderRadius: 8, padding: 12,
<div style={{ fontSize: 10, color: "#f5c842", letterSpacing: 2, m
{tips.map((tip, ti) => (
<div key={ti} style={{ fontSize: 12, color: "#bbb", marginBotto
→ {tip}
</div>
))}
</div>
)}
</>
)}
{!result && (
<div style={{ fontSize: 12, color: "#555", fontFamily: "Barlow, sans-se
Enter your {f.label.toLowerCase()} above to see your percentile and t
</div>
)}
</div>
);
})}
</div>
);
})()}
{tab === "Nutrition" && (() => {
const [mealsDone, setMealsDone] = useState(() => {
try {
const s = localStorage.getItem("rb_meals");
if (!s) return {};
const parsed = JSON.parse(s);
const today = new Date().toISOString().split("T")[0];
return parsed.date === today ? parsed.data : {};
} catch { return {}; }
});
const [water, setWater] = useState(() => {
try {
const s = localStorage.getItem("rb_water");
if (!s) return 0;
const parsed = JSON.parse(s);
const today = new Date().toISOString().split("T")[0];
return parsed.date === today ? parsed.count : 0;
} catch { return 0; }
});
const toggleMeal = (meal) => {
const next = { ...mealsDone, [meal]: !mealsDone[meal] };
setMealsDone(next);
localStorage.setItem("rb_meals", JSON.stringify({ date: new Date().toISOString().
};
const addWater = () => {
const next = Math.min(water + 1, 12);
setWater(next);
localStorage.setItem("rb_water", JSON.stringify({ date: new Date().toISOString().
};
const removeWater = () => {
const next = Math.max(water - 1, 0);
setWater(next);
localStorage.setItem("rb_water", JSON.stringify({ date: new Date().toISOString().
};
const mealsHit = Object.values(mealsDone).filter(Boolean).length;
return (
<div>
</div>
<div style={{ fontSize: 11, color: "#888", letterSpacing: 3, marginBottom: 4 }}
<div style={{ fontSize: 12, color: "#666", marginBottom: 16, fontFamily: Check off each meal as you eat it. Consistency builds champions.
"Barlo
{/* Meals eaten today */}
<div style={{
background: "#111118", border: "1px solid #1e1e2e",
borderRadius: 12, padding: 14, marginBottom: 14,
display: "flex", justifyContent: "space-between", alignItems: "center",
}}>
<div>
</div>
</div>
<div style={{ fontSize: mealsHit >= 4 ? 36 : 24 }}>{mealsHit >= 4 ? " </div>
<div style={{ fontSize: 11, color: "#888", letterSpacing: 2 }}>MEALS TODAY<
<div style={{ fontSize: 28, fontWeight: 900, color: mealsHit >= 4 ? "#22c55
{mealsHit} / {NUTRITION.length}
" : "
{NUTRITION.map((n, i) => (
<div key={i} onClick={() => toggleMeal(n.meal)} style={{
background: mealsDone[n.meal] ? "#0d1f0d" : "#111118",
border: `1px solid ${mealsDone[n.meal] ? "#22c55e" : "#1e1e2e"}`,
borderRadius: 12, padding: 14, marginBottom: 10, cursor: "pointer",
transition: "all 0.2s",
}}>
<div style={{ display: "flex", justifyContent: "space-between", alignItems:
<div style={{ flex: 1 }}>
<div style={{ display: "flex", alignItems: "center", gap: 8, marginBott
<div style={{
width: 22, height: 22, borderRadius: "50%",
border: `2px solid ${mealsDone[n.meal] ? "#22c55e" : "#444"}`,
background: mealsDone[n.meal] ? "#22c55e" : "transparent",
display: "flex", alignItems: "center", justifyContent: "center",
fontSize: 12, flexShrink: 0, color: "#000", fontWeight: 900,
}}>
{mealsDone[n.meal] ? "✓" : ""}
</div>
<div style={{ fontSize: 11, color: mealsDone[n.meal] ? "#22c55e" : "#
</div>
<div style={{ fontSize: 14, fontWeight: 700, marginBottom: 4, marginLef
<div style={{ fontSize: 12, color: "#888", fontFamily: "Barlow, sans-se
</div>
</div>
</div>
))}
{/* Water tracker */}
<div style={{
background: "#0a1220", border: "1px solid #3b82f6",
borderRadius: 12, padding: 16, marginBottom: 14,
}}>
<div style={{ fontSize: 11, color: "#3b82f6", letterSpacing: 3, marginBottom:
<div style={{ fontSize: 12, color: "#888", marginBottom: 12, fontFamily: "Bar
Goal: 9 cups (you need ~69oz daily at 138 lbs)
</div>
<div style={{ display: "flex", flexWrap: "wrap", gap: 8, marginBottom: {Array.from({ length: 9 }).map((_, i) => (
<div key={i} style={{
fontSize: 22, opacity: i < water ? 1 : 0.2,
transition: "opacity 0.2s",
}}> </div>
14 }}>
))}
</div>
<div style={{ display: "flex", gap: 10, alignItems: "center" }}>
<button onClick={removeWater} style={{
background: "#1e2a4a", border: "1px solid #3b82f6", color: "#3b82f6",
borderRadius: 8, padding: "10px 20px", fontWeight: 900, fontSize: 18,
cursor: "pointer", fontFamily: "inherit",
}}>−</button>
<div style={{ flex: 1, textAlign: "center" }}>
<div style={{ fontSize: 28, fontWeight: 900, color: water >= 9 ? "#22c55e
<div style={{ fontSize: 11, color: "#666" }}>{water >= 9 ? "GOAL HIT "
</div>
<button onClick={addWater} style={{
background: "#1e2a4a", border: "1px solid #3b82f6", color: "#3b82f6",
borderRadius: 8, padding: "10px 20px", fontWeight: 900, fontSize: 18,
cursor: "pointer", fontFamily: "inherit",
}}>+</button>
</div>
</div>
{/* Avoid list */}
<div style={{
background: "linear-gradient(135deg, #1a0d00, #2d1a00)",
border: "1px solid #f97316", borderRadius: 12, padding: 14,
}}>
meals"
<div style={{ fontSize: 13, fontWeight: 700, color: "#f97316", marginBottom:
{["Soda & energy drinks", "Fast food more than once a week", "Skipping <div key={i} style={{ fontSize: 13, color: "#bbb", marginBottom: 4, fontFam
✗ {item}
</div>
))}
</div>
</div>
);
})()}
{tab === "Legends" && (() => {
const [selectedRB, setSelectedRB] = useState(0);
const [studiedPlayers, setStudiedPlayers] = useState(() => {
try { return JSON.parse(localStorage.getItem("rb_studied") || "{}"); } catch { re
});
const DETAILED_RBS = [
{
name: "Darren Sproles", height: "5'6\"", weight: "190 lbs", nflYears: "2005–201
superpower: "Elusiveness & quickness in space",
note: "Smaller than you right now. Became one of the most elusive RBs ever. 3x
lessons: ["Use your low center of gravity as a weapon", "Be the quickest watchFor: "How he makes defenders miss in open space with minimal effort",
guy on
},
{
name: "Barry Sanders", height: "5'8\"", weight: "200 lbs", nflYears: "1989–1998
superpower: "Cuts so sharp he was basically impossible to tackle",
note: "Considered the greatest RB of all time by many. Never the biggest. Just
lessons: ["Plant your foot and cut hard — don't round corners", "Keep your legs
watchFor: "How he stops on a dime and explodes in a completely different direct
},
{
name: "Emmitt Smith", height: "5'9\"", weight: "216 lbs", nflYears: "1990–2004"
superpower: "Consistency and football IQ",
note: "All-time NFL rushing leader. Wasn't the fastest. Wasn't the biggest. Jus
lessons: ["Read your blockers — let them open holes for you", "Protect the ball
watchFor: "How he reads the offensive line and finds cutback lanes",
},
{
name: "Maurice Jones-Drew", height: "5'7\"", weight: "208 lbs", nflYears: "2006
superpower: "Power despite small size — impossible to bring down",
note: "Low center of gravity meant defenders couldn't get leverage on him. Buil
lessons: ["Get low on contact — be harder to tackle than you look", "Strength t
watchFor: "How he keeps his legs churning and breaks arm tackles",
},
];
const rb = DETAILED_RBS[selectedRB];
const toggleStudied = (name) => {
const next = { ...studiedPlayers, [name]: !studiedPlayers[name] };
setStudiedPlayers(next);
localStorage.setItem("rb_studied", JSON.stringify(next));
};
return (
<div>
</div>
<div style={{ fontSize: 11, color: "#888", letterSpacing: 3, marginBottom: 4 }}
<div style={{ fontSize: 12, color: "#666", marginBottom: 16, fontFamily: Tap a player to study them. They said these guys were too small too.
"Barlo
{/* Player selector */}
<div style={{ display: "flex", gap: 8, overflowX: "auto", paddingBottom: {DETAILED_RBS.map((rb, i) => (
<button key={i} onClick={() => setSelectedRB(i)} style={{
background: selectedRB === i ? "#f5c842" : "#111118",
color: selectedRB === i ? "#000" : "#888",
border: `1px solid ${selectedRB === i ? "#f5c842" : "#1e1e2e"}`,
borderRadius: 8, padding: "8px 12px", fontWeight: 700,
fontSize: 11, cursor: "pointer", whiteSpace: "nowrap",
fontFamily: "inherit", letterSpacing: 1,
}}>
{studiedPlayers[rb.name] ? "✓ " : ""}{rb.name.split(" ")[1].toUpperCase()
</button>
))}
8, mar
</div>
{/* Player card */}
<div style={{
background: "linear-gradient(135deg, #1a1400, #111118)",
border: "2px solid #f5c842", borderRadius: 16, padding: 18, marginBottom: 14,
}}>
}}>
"NFL Y
<div style={{ fontSize: 28, fontWeight: 900, color: "#f5c842" }}>{rb.name}</d
<div style={{ display: "flex", gap: 16, marginTop: 8, marginBottom: 12 {[{ l: "HEIGHT", v: rb.height }, { l: "WEIGHT", v: rb.weight }, { l: <div key={s.l}>
<div style={{ fontSize: 9, color: "#888", letterSpacing: 2 }}>{s.l}</di
<div style={{ fontSize: 14, fontWeight: 700, color: "#f0f0f0" }}>{s.v}<
</div>
))}
</div>
<div style={{ background: "#1a1a00", borderRadius: 8, padding: 10, marginBott
<div style={{ fontSize: 10, color: "#f5c842", letterSpacing: 2, marginBotto
<div style={{ fontSize: 14, fontWeight: 700 }}>{rb.superpower}</div>
</div>
<div style={{ fontSize: 13, color: "#bbb", fontFamily: "Barlow, sans-serif",
{/* Lessons */}
<div style={{ marginBottom: 12 }}>
<div style={{ fontSize: 10, color: "#f5c842", letterSpacing: 2, marginBotto
{rb.lessons.map((l, i) => (
<div key={i} style={{
background: "#111118", borderRadius: 8, padding: "8px 12px",
marginBottom: 6, fontSize: 13, color: "#e0e0e0", fontFamily: "Barlow, s
}}>
→ {l}
</div>
))}
</div>
{/* Watch for */}
<div style={{ background: "#0a1220", borderRadius: 8, padding: 10, marginBott
<div style={{ fontSize: 10, color: "#3b82f6", letterSpacing: 2, marginBotto
<div style={{ fontSize: 13, color: "#bbb", fontFamily: "Barlow, sans-serif"
</div>
<button onClick={() => toggleStudied(rb.name)} style={{
background: studiedPlayers[rb.name] ? "#22c55e" : "#1a1400",
border: `2px solid ${studiedPlayers[rb.name] ? "#22c55e" : "#f5c842"}`,
color: studiedPlayers[rb.name] ? "#000" : "#f5c842",
borderRadius: 8, padding: "12px", fontWeight: 900,
fontSize: 14, cursor: "pointer", width: "100%",
fontFamily: "inherit", letterSpacing: 1,
}}>
{studiedPlayers[rb.name] ? "✓ STUDIED" : "MARK AS STUDIED"}
</button>
</div>
{/* Progress */}
<div style={{ background: "#111118", border: "1px solid #1e1e2e", borderRadius:
<div style={{ fontSize: 11, color: "#888", letterSpacing: 2, marginBottom: 8
<div style={{ display: "flex", gap: 8 }}>
{DETAILED_RBS.map((rb, i) => (
<div key={i} style={{
flex: 1, height: 8, borderRadius: 4,
background: studiedPlayers[rb.name] ? "#f5c842" : "#1e1e2e",
transition: "background 0.3s",
}} />
))}
</div>
<div style={{ fontSize: 12, color: "#666", marginTop: 6, fontFamily: "Barlow,
{Object.values(studiedPlayers).filter(Boolean).length} / {DETAILED_RBS.leng
</div>
</div>
</div>
);
})()}
</div>
{/* Bottom Nav */}
<div style={{
position: "fixed", bottom: 0, left: 0, right: 0,
background: "#0f1117", borderTop: "2px solid #1e1e2e",
display: "flex", padding: "8px 0",
}}>
{tabs.map(t => (
<button key={t} onClick={() => setTab(t)} style={{
flex: 1, background: "none", border: "none",
color: tab === t ? "#f5c842" : "#444",
fontSize: 9, fontWeight: 700, letterSpacing: 1,
cursor: "pointer", fontFamily: "inherit",
padding: "4px 2px",
borderTop: tab === t ? "2px solid #f5c842" : "2px solid transparent",
}}>
{t === "Home" ? " <br />{t.toUpperCase()}
</button>
" : t === "Today" ? " " : t === "Program" ? " " : t === "Ben
))}
</div>
}
);
</div>